export const whiteSort = data => data.filter(item => item.color === "white")

export const yellowSort = data => data.filter(item => item.color === "yellow")

export const redSort = data => data.filter(item => item.color === "red")

export const blueSort = data => data.filter(item => item.color === "blue")

export const biegeSort = data => data.filter(item => item.color === "biege")
