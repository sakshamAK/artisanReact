
export const filterByPaperCuttings = data => data.filter(item => item.type === "PAPER CUTTING")

export const filterByshadowBox = data => data.filter(item => item.type === "SHADOW BOX")

export const filterByQuilling = data => data.filter(item => item.type === "QUILLING")

export const filterByLayeringArt = data => data.filter(item => item.type === "LAYERING ART")

export const filterByOrigami = data => data.filter(item => item.type === "ORIGAMI")

export const filterByKirigami = data => data.filter(item => item.type === "KIRIGAMI")